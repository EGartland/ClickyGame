import React from "react";
import "./Title.css";

const Title = () => {
    return (


        <div className='parallax'>
            <div className='section no-pad-bot'>
                <div className='container'>
                    <h2 className='header'>Click on the puppies to boost your score,< br />but don't click the same puppy more than once!</h2>
                </div>
            </div>
        </div>

    );
};

export default Title;